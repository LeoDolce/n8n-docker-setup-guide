# 3. Docker Validation

## Open Docker Desktop

Wait until the status below appears:

Docker Engine Running

## Validate Installation

Open CMD or PowerShell and run:

```bash
docker --version
Expected Result

The terminal should return something similar to:

Docker version XX.XX.X